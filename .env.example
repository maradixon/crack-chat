# Copy this file to ".env" and paste your key from https://console.anthropic.com
# (Set a monthly spending cap there too, so it can never surprise you.)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: change the port the app runs on (default 3000)
# PORT=3000
